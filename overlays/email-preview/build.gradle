import org.apereo.portal.start.gradle.plugins.GradlePlutoPlugin

apply plugin: GradlePlutoPlugin

dependencies {
    runtime "org.jasig.portlet:email-preview:${emailPreviewPortletVersion}@war"
}

war {
    archiveName 'email-preview.war'
}
