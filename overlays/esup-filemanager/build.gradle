import org.apereo.portal.start.gradle.plugins.GradlePlutoPlugin

apply plugin: GradlePlutoPlugin

dependencies {
    runtimeOnly "org.esupportail.portlet.filemanager:esup-filemanager:${esupFilemanagerPortletVersion}@war"
}

war {
    archiveFileName.set('esup-filemanager.war')
}
