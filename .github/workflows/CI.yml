name: CI
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    name: "${{ matrix.platform }} with Java ${{ matrix.java-distribution }} version 8"
    strategy:
      matrix:
        platform:
          - ubuntu-latest
          - windows-latest
        java-distribution:
          - adopt-hotspot
          - temurin
          - zulu
    runs-on: ${{ matrix.platform }}
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: ${{ matrix.java-distribution }}
          java-version: 8
      - name: Initialize Portal
        run: ./gradlew -i -S portalInit
      - name: Start Tomcat
        run: ./gradlew -i -S tomcatStart
      - name: Run the end-to-end tests
        run: ./gradlew -i -S playwrightRun
      - name: Stop Database
        run: ./gradlew -i -S hsqlStop
      - name: Stop Tomcat
        run: ./gradlew -i -S tomcatStop
      - name: Clear Logs Tomcat
        run: ./gradlew -i -S tomcatClearLogs
      - name: Generate Skin
        run: ./gradlew -i -S skinGenerate -DskinName=test
